server {
	listen 80;

	# Single-page app: unknown paths fall back to index.html so client-side
	# routes like /device/:id resolve on a hard refresh.
	location / {
		root /usr/share/nginx/html;
		try_files $uri /index.html;
	}
}
